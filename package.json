{
  "name": "dogemr-app",
  "version": "1.0.0",
  "description": "EMR for dogs to stay up to date to expedite veterinarian visits and catalogue their health records.",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js", 
    "build": "cd client && npm install && npm run build", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }, 
  "proxy": "http://localhost:3001", 
  "engines": {
    "node": "19.9.0"
  }
}
